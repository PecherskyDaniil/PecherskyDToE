
`Привет, бро!`

Близится новый год, а я **заболел**. Выручай!
У нас проблема на `ПреПрод` стенде. Мы запустили приложение и Заказчик начал его тестировать. Однако, часть функционала работает 
не корректно с точки зрения бизнес поведения. Нам нужно отладить приложение, а для этого вновь нужна твоя помощь.

Нам необходимо сделать следующее:
1. Создать систему логирования на основе шаблона "Наблюдатель"
2. Логирование должно иметь минимум три уровня: `Error`, `Warning`, `Information`
3. Необходимо логировать в зависимости от настроек. Уровень логирование указывается в настройках и режим: `Консоль`, `Файл`
4. Если указан режим логирования в файл, то система на каждый день должна создавать отдельный файл в указанном каталоге.
5. **Важно!** Если по Api приходит запрос: `DELETE`, `PUT`, `PATH` то нужно обязательно `request` структуру в лог добавлять.
6. **Желательно!** В настройках указывать формат вывода сообщений. Формат даты в сообщение и прочее.

В прошлый раз ты не успел сделать, я понимаю, свои дела. Однако сейчас, команда остро нуждается в твоей помощи.

